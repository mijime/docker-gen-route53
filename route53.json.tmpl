{
"Changes": [
{{ $first := 1 }}
{{ $data := groupByMulti $ "Env.VIRTUAL_HOST" "," }}
{{ range $host, $containers := $data }}
{{ range $container := $containers }}
{{ if eq $first 0}},{{ end }}
    {
        "Action": "UPSERT",
        "ResourceRecordSet": {
        "Name": "{{$host}}",
        "Type": "CNAME",
        "TTL": 300,
        "ResourceRecords": [
        {
        "Value": "{{$.Env.SELF_HOST}}"
        }
        ]
        }
    }
{{ $first := 0 }}
{{ end }}
{{ end }}
]
}
